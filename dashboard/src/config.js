// Single place to point the frontend at Team 3's backend.
// If the FastAPI server runs somewhere other than localhost (e.g. on a
// Jetson/Raspberry Pi on the same network), change this one line.
export const API_BASE_URL = 'http://localhost:8000'
